class User {
    constructor(id, ownerId, realname,imageUrl, address, phone) {
        this.id = id;
        this.ownerId = ownerId;
        this.realname = realname;
        this.imageUrl = imageUrl;
        this.address = address;
        this.phone = phone;
    }
}

export default User;